{
  "name": "freelens-registry-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for FreeLens Extension Registry API",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
